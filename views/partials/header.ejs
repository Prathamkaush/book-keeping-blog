<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">
    <link rel="icon" type="image/png" href="images/books.png">
    <link rel="stylesheet" href="styles/main.css">
    
        <title> Books</title>
</head>
<body>
    <nav class="navbar navbar-expand-lg bg-dark "  data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">📚 Books</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
  Genres
</a>
<ul class="dropdown-menu">
  <li><a class="dropdown-item"  href="/books/genre/Mystery">Mystery</a></li>
  <li><a class="dropdown-item" href="/books/genre/Science Fiction">Science Fiction</a></li>
  <li><a class="dropdown-item" href="/books/genre/Romance">Romance</a></li>
  <li><a class="dropdown-item" href="/books/genre/Historical Fiction">Historical Fiction</a></li>
  <li><a class="dropdown-item" href="/books/genre/Fantasy">Fantasy</a></li>
  <li><a class="dropdown-item" href="/books/genre/Self-help book">Self-help book</a></li>
</ul>

        </li>
        <% if (isAdmin == false) { %>
         <li class="nav-item">
  <a href="/" class="nav-link" onclick="adminLogin()">Admin Login</a>
</li>
<%}%>
<% if (isAdmin) { %>
  <li class="nav-item">
  <a href="/" id="logoutBtn" class="nav-link btn btn-danger">Logout</a>
</li>
<% } %>
      </ul>
      <form class="d-flex" role="search" method="GET" action="/search">
  <input class="form-control me-2" type="search" name="q" placeholder="Search books..." aria-label="Search" />
  <button class="btn btn-outline-success" type="submit">Search</button>
</form>

    </div>
  </div>
</nav>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/js/bootstrap.bundle.min.js" integrity="sha384-ndDqU0Gzau9qJ1lfW4pNLlhNTkCfHzAVBReH9diLvGRem5+R9g2FzA8ZGN954O5Q" crossorigin="anonymous"></script>    
<script>
  function adminLogin() {
    const password = prompt("Enter Admin Password:");  // ✅ capture password
    if (!password) return;  // if user cancelled

    fetch("/admin-login", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ password })   // ✅ now password is defined
    })
    .then(res => res.json())
    .then(data => {
      if (data.success) {
        alert("Admin logged in!");
        location.reload(); // reload so admin buttons show up
      } else {
        alert("Wrong password!");
      }
    });
  }
</script>
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const logoutBtn = document.getElementById("logoutBtn");
    if (logoutBtn) {
      logoutBtn.addEventListener("click", () => {
        fetch("/admin-logout", { method: "POST" })
          .then(res => res.json())
          .then(data => {
            if (data.success) {
              alert("Logged out successfully!");
              window.location.reload(); // refresh page → buttons hidden
            }
          });
      });
    }
  });
</script>

</body>
</html>